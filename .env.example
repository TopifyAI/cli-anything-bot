# GitHub Bot Account Personal Access Token (classic)
# Required scopes: repo, notifications
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Bot's GitHub username (e.g., cli-anything-bot)
BOT_USERNAME=cli-anything-bot

# Anthropic API Key for Claude
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Claude model to use (default: claude-sonnet-4-20250514)
CLAUDE_MODEL=claude-sonnet-4-20250514

# Polling interval in seconds (default: 30)
POLL_INTERVAL=30

# Working directory for cloned repos (default: /tmp/cli-anything-bot)
WORK_DIR=/tmp/cli-anything-bot

# Log level (default: INFO)
LOG_LEVEL=INFO

# --- Abuse Prevention ---

# Access mode: "open" (anyone), "users" (allowlist), "repos" (allowlist)
ACCESS_MODE=users

# Comma-separated allowed GitHub usernames (when ACCESS_MODE=users)
ALLOWED_USERS=your-username,teammate

# Comma-separated allowed repos (when ACCESS_MODE=repos)
# ALLOWED_REPOS=owner/repo1,owner/repo2

# Max builds per user per day (0 = unlimited)
DAILY_LIMIT_PER_USER=3

# Max builds globally per day (0 = unlimited)
DAILY_LIMIT_GLOBAL=20
